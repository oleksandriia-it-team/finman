import { type ButtonVariant } from '@frontend/ui/ui-button/props/button.props';

export interface FinResponsiveMenuItemProps {
  icon: string;
  name: string;
  className?: string;
  variant?: ButtonVariant;
}
