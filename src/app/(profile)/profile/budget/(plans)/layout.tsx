'use client';

import type { ChildrenComponentProps } from 'src/client/shared/models/component-with-chilren.model';

export default function BudgetPlansLayout({ children }: ChildrenComponentProps) {
  return <>{children}</>;
}
